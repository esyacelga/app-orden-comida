package com.orden.comida.app.dominio.objetovalor;

import com.orden.comida.app.dominio.objetovalor.IDBase;

public class IDOrdenItem extends IDBase<Long> {
    public IDOrdenItem(Long value) {
        super(value);
    }
}
